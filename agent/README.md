# vscode-onedrive-sync-router agent

This folder contains the local FastAPI agent, SQLite queue, rule engine, and provider abstraction for vscode-onedrive-sync-router.

The default behavior is dry-run local-first processing. The agent is not responsible for controlling native OneDrive sync.
