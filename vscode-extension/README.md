# VS Code OneDrive Sync Router

This extension sends local VS Code file activity to the vscode-onedrive-sync-router agent and displays local status.

The extension identifier is vscode-onedrive-sync-router. The project is local-first and does not perform real cloud writes in the MVP.
